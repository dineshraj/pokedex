export interface KantoPokedex {
  entry_number: number;
  name: string;
  url: string;
}

export interface localStorageDataModel {
  entry_number: number;
  name: string;
  flavorText: string;
  soundFile: string;
  sprite: string;
}
